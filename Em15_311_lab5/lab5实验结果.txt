

实验的串口输出如“终端显示.png”图所示，同时“板子运行过程.MOV”录下了板子的运行情况。

运行时，4个LED会以计数的形式亮起，开关状态指示LED灯亮起的延时秒数。比如右边两个开关被推上去了，那么LED灯的亮暗状况就会每隔3秒变一次。每当开关状态有变化，计时器就被重置，即LED的延时秒数被更新为当前的开关状态所指示数字，且LED重新从零开始计数；同时串口会显示“DIP Switch Status 之前的开关状态, 现在的开关状态"。

如果按下最右边的按钮，计时器停止，程序结束。

